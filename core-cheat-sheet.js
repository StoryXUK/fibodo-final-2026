const counters=document.querySelectorAll('[data-count-to]');const formatGBP=v=>`£${Math.round(v)}`;function animateCounter(el){const target=Number(el.dataset.countTo||0),duration=1100,start=performance.now();function tick(now){const p=Math.min((now-start)/duration,1),e=1-Math.pow(1-p,3);el.textContent=formatGBP(target*e);if(p<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;animateCounter(entry.target);observer.unobserve(entry.target)})},{threshold:.45});counters.forEach(counter=>observer.observe(counter));
